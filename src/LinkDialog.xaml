<Window x:Class="NativeMDView.LinkDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Insert Link" 
        Height="200" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#2D2D30"
        Foreground="#D4D4D4">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <TextBlock Grid.Row="0" Grid.Column="0" Text="Text:" VerticalAlignment="Center" Margin="0,0,10,10"/>
        <TextBox x:Name="LinkTextBox" Grid.Row="0" Grid.Column="1" Margin="0,0,0,10" 
                 Background="#1E1E1E" Foreground="#D4D4D4" BorderBrush="#3E3E42" Padding="5"/>
        
        <TextBlock Grid.Row="1" Grid.Column="0" Text="URL:" VerticalAlignment="Center" Margin="0,0,10,0"/>
        <TextBox x:Name="LinkUrlBox" Grid.Row="1" Grid.Column="1" 
                 Background="#1E1E1E" Foreground="#D4D4D4" BorderBrush="#3E3E42" Padding="5"/>
        
        <StackPanel Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="OK" Width="75" Height="25" Margin="0,0,10,0" Click="Ok_Click"
                    Background="#0E639C" Foreground="White" BorderThickness="0" Cursor="Hand"/>
            <Button Content="Cancel" Width="75" Height="25" Click="Cancel_Click"
                    Background="#3E3E42" Foreground="White" BorderThickness="0" Cursor="Hand"/>
        </StackPanel>
    </Grid>
</Window>
